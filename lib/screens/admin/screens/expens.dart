import 'package:flutter/material.dart';

Widget Expense() {
  return Container(
    child: Column(
      children: [
        Container(
          width: 500,
          height: 400,
          color: Colors.red,
          child: Text('empo;expense'),
        )
      ],
    ),
  );
}
